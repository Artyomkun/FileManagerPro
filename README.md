# 📁 File Manager Pro - Кросс-языковый файловый менеджер

**Профессиональный файловый менеджер, разработанный на трёх языках программирования (C, C++, C#) с полной интеграцией и установщиком.**

---

## 🎯 **Цель проекта**

Демонстрация навыков работы с:

- **C** — низкоуровневая работа с файловой системой (WinAPI)
- **C++** — логика, алгоритмы, ООП-архитектура
- **C#** — современный графический интерфейс (WPF)
- **Межъязыковое взаимодействие** — P/Invoke, DLL, маршалинг данных
- **Профессиональная упаковка** — установщик Inno Setup

---

## 📂 **Структура проекта**

D:\c\FileManagerProject\
│
├── 📁 Installer_CSharp\                    # Установщик (Inno Setup)
│   ├── setup.iss                          # Скрипт установки
│   ├── build.bat                          # Скрипт сборки
│   ├── License.txt                        # Лицензионное соглашение
│   └── 📁 GUI_CSharp\                     # Графический интерфейс (C# WPF)
│       ├── FileManager.csproj             # Проект C#
│       ├── MainWindow.xaml                # Разметка главного окна
│       ├── MainWindow.xaml.cs             # Логика главного окна
│       └── 📁 LogicLib_Cpp\               # Логика навигации (C++)
│           ├── navigator.cpp              # Реализация навигатора
│           ├── navigator.hpp              # Заголовочный файл C++
│           └── 📁 CoreLib_C\              # Ядро системы (C)
│               ├── file_ops.c             # Файловые операции
│               ├── file_ops.h             # Заголовочный файл C
│               └── file_utils.c           # Вспомогательные функции
│
└── 📄 README.md                           # Эта документация

---

## 🏗️ **Архитектура**

### **Трёхуровневая архитектура:**

┌─────────────────────────────────────────────────┐
│                    C# (GUI)                     │
│              WPF • MVVM • P/Invoke              │
├─────────────────────────────────────────────────┤
│                    C++ (Logic)                  │
│        OOP • Algorithms • Data Structures       │
├─────────────────────────────────────────────────┤
│                     C (Core)                    │
│            WinAPI • System Calls • I/O          │
└─────────────────────────────────────────────────┘

### **Взаимодействие компонентов:**

1. **C# → C++** через P/Invoke (Platform Invocation Services)
2. **C++ → C** через прямой вызов функций и линковку
3. **C → Windows API** через системные вызовы

---

## ⚙️ **Технологический стек**

| Слой        | Технологии             | Назначение                  |
|-------------|------------------------|-----------------------------|

| **GUI**     | C#, WPF, XAML, MVVM     | Пользовательский интерфейс |
| **Logic**   | C++17, STL, OOP         | Бизнес-логика, алгоритмы   |
| **Core**    | C11, WinAPI             | Низкоуровневые операции    |
| **Build**   | GCC, dotnet             | Компиляция и сборка        |
| **Package** | Inno Setup, VC++ Redist | Упаковка и распространение |

---

## 🚀 **Быстрый старт**

### **Предварительные требования:**

1. **Windows 10/11** (64-bit)
2. **Visual Studio 2022** или **MinGW** + **.NET 6.0 SDK**
3. **Inno Setup 6** (для сборки установщика)

### **Сборка проекта:**

```bash
# 1. Сборка C-ядра
cd GUI_CSharp\LogicLib_Cpp\CoreLib_C
gcc -c file_ops.c file_utils.c
gcc -shared -o CoreLib_C.dll *.o -luser32 -lkernel32

# 2. Сборка C++-логики
cd ..
gcc -c navigator.cpp -o navigator.o -I"./CoreLib_C" 
gcc -shared -o LogicLib_Cpp.dll navigator.o -L"./CoreLib_C" -lCoreLib_C

# 3. Сборка C#-интерфейса
cd ../..
dotnet build -c Release

# 4. Сборка установщика
cd ../Installer_CSharp
build.bat
```

### **Запуск:**

1. Запустите `Installer_CSharp\Output\FileManagerPro_Setup.exe`
2. Следуйте инструкциям установщика
3. Запустите File Manager Pro из меню Пуск

---

## 🔧 **Разработка**

### **Среда разработки:**

- **C# часть:** Visual Studio 2022 с .NET 6.0
- **C/C++ часть:** Visual Studio Code с расширениями C/C++
- **Установщик:** Inno Setup Compiler

### **Настройка проекта C++ для Visual Studio:**

1. Создайте проект "Динамическая библиотека DLL"
2. Добавьте файлы из `LogicLib_Cpp\` и `CoreLib_C\`
3. Настройте пути включения и библиотек

### **Полезные команды для разработки:**

```bash
# Проверка экспортируемых функций DLL
dumpbin /exports LogicLib_Cpp.dll

# Зависимости DLL
dumpbin /dependents FileManager.exe

# Отладка P/Invoke
[System.Runtime.InteropServices.DllImport("LogicLib_Cpp.dll", 
    EntryPoint = "NavigateTo", 
    CallingConvention = CallingConvention.Cdecl,
    CharSet = CharSet.Ansi)]
```

---

## 📖 **Документация API**

### **CoreLib_C (C API):**

```c
// Основные функции
int list_files(const char* path, FileInfo** files, int* count);
int copy_file(const char* src, const char* dst);
int delete_path(const char* path);
int create_directory(const char* path);
int get_file_size(const char* path);
```

### **LogicLib_Cpp (C++ API):**

```cpp
// Класс навигатора
class FileNavigator {
public:
    bool change_directory(const std::string& path);
    std::vector<FileItem> get_directory_contents(bool force_refresh = false);
    bool search_file(const std::string& filename, 
                     std::vector<SearchResult>& results,
                     bool recursive = false);
    DiskInfo get_disk_info(const std::string& path = "");
};
```

### **C# P/Invoke интерфейс:**

```csharp
public class FileManagerInterop {
    [DllImport("LogicLib_Cpp.dll", CallingConvention = CallingConvention.Cdecl)]
    public static extern IntPtr CreateNavigator();
    
    [DllImport("LogicLib_Cpp.dll", CallingConvention = CallingConvention.Cdecl)]
    [return: MarshalAs(UnmanagedType.Bool)]
    public static extern bool NavigateTo(IntPtr navigator, string path);
    
    [DllImport("LogicLib_Cpp.dll", CallingConvention = CallingConvention.Cdecl)]
    public static extern void DeleteNavigator(IntPtr navigator);
}
```

---

## 🧪 **Тестирование**

### **Модульные тесты:**

```csharp
[TestClass]
public class FileNavigatorTests {
    [TestMethod]
    public void Test_NavigateTo_ValidPath_ReturnsTrue() {
        IntPtr navigator = FileManagerInterop.CreateNavigator();
        bool result = FileManagerInterop.NavigateTo(navigator, "C:\\Windows");
        Assert.IsTrue(result);
        FileManagerInterop.DeleteNavigator(navigator);
    }
}
```

### **Интеграционные тесты:**

```bash
# Тест C библиотеки
test_core.exe C:\\Windows

# Тест C++ библиотеки  
test_navigator.exe --path D:\\ --search "*.txt"
```

---

## 🔍 **Отладка**

### **Распространённые проблемы:**

1. **DLL не найдена:** Проверьте пути и архитектуру (x64/x86)
2. **Ошибка P/Invoke:** Проверьте сигнатуры функций и calling convention
3. **Утечки памяти:** Используйте `valgrind` (C/C++) и профилировщик .NET

### **Инструменты отладки:**

- **C/C++:** WinDbg, Visual Studio Debugger, Dependency Walker
- **C#:** Visual Studio Debugger, dotTrace, ANTS Performance Profiler
- **Интеграция:** Mixed-mode debugging в Visual Studio

---

## 📈 **Производительность**

### **Оптимизации:**

- **Кэширование:** Результаты запросов к файловой системе
- **Асинхронность:** Неблокирующий UI в C# через async/await
- **Пул объектов:** Переиспользование структур данных
- **Пакетные операции:** Минимизация межъязыковых вызовов

### **Метрики:**

- Время отклика UI: < 50 мс
- Загрузка директории (1000 файлов): < 500 мс
- Поиск по файловой системе: < 1 сек

---

## 🔒 **Безопасность**

### **Защитные меры:**

- Валидация путей (предотвращение Path Traversal)
- Проверка прав доступа перед операциями
- Безопасное освобождение ресурсов
- Логирование ошибок без утечки информации

### **Обработка ошибок:**

```csharp
try {
    IntPtr navigator = FileManagerInterop.CreateNavigator();
    // ...
} catch (DllNotFoundException ex) {
    Logger.Error($"DLL not found: {ex.Message}");
    ShowErrorMessage("Требуемые библиотеки не найдены");
} catch (EntryPointNotFoundException ex) {
    Logger.Error($"Function not found: {ex.Message}");
    ShowErrorMessage("Версия библиотеки несовместима");
}
```

---

## 📦 **Распространение**

### **Требования к развёртыванию:**

1. **VC++ Redistributable 2015-2022** (x64)
2. **.NET 6.0 Desktop Runtime** (x64)
3. **Windows 10+** (64-bit)

### **Тихая установка:**

```powershell
FileManagerPro_Setup.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /LOG="install.log"
```

### **Параметры командной строки:**

- `/DIR="C:\Program Files\FileManager"` — путь установки
- `/COMPONENTS="main,explorer_integration"` — выбор компонентов
- `/NOCONTEXTMENU` — не добавлять в контекстное меню

---

## 🤝 **Вклад в проект**

### **Руководство по стилю:**

- **C:** C11 стандарт, префикс `file_` для функций
- **C++:** C++17, Google Style Guide, RAII
- **C#:** .NET 6, async/await, MVVM, dependency injection

### **Процесс внесения изменений:**

1. Форк репозитория
2. Создайте ветку для новой функции
3. Добавьте тесты для новой функциональности
4. Создайте Pull Request с описанием изменений

---

## 📄 **Лицензия**

MIT License

Copyright (c) 2024 FileManager Pro Team

Разрешается свободное использование, копирование, модификация,
распространение при условии сохранения уведомления об авторских правах.

ПО ПРЕДОСТАВЛЯЕТСЯ "КАК ЕСТЬ", БЕЗ КАКИХ-ЛИБО ГАРАНТИЙ.

---

## 📞 **Поддержка и обратная связь**

- **Вопросы по использованию:** `{app}\Help\index.html`
- **Сообщения об ошибках:** GitHub Issues
- **Предложения функций:** Форумы разработчиков
- **Контакты:** <support@filemanager.example.com>

---

## 🏆 **Преимущества проекта**

✅ **Полный стек технологий** — от WinAPI до современного WPF  
✅ **Кросс-языковая интеграция** — реальный пример взаимодействия C/C++/C#  
✅ **Профессиональная упаковка** — готовый к распространению установщик  
✅ **Производительность** — оптимизировано для работы с большими каталогами  
✅ **Безопасность** — защита от распространённых уязвимостей файловых менеджеров  
✅ **Расширяемость** — модульная архитектура позволяет добавлять плагины  

---

## 🎓 **Образовательная ценность**

Этот проект демонстрирует:

1. **Архитектурные навыки** — проектирование многоуровневой системы
2. **Межъязыковое взаимодействие** — интеграция управляемого и неуправляемого кода
3. **Системное программирование** — работа с файловой системой Windows
4. **Профессиональную разработку** — сборка, упаковка, документация
5. **Оптимизацию производительности** — кэширование, асинхронность, пулы объектов

---

*Проект разработан для демонстрации практических навыков работы с C, C++ и C# в реальном приложении.*
