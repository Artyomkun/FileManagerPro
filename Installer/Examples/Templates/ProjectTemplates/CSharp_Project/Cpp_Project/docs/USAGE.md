# Использование FileManager (C++ версия)

## 🚀 Быстрый старт

### Сборка программы

```bash
# Создание директории для сборки
mkdir build
cd build

# Конфигурация проекта
cmake ..

# Компиляция
cmake --build .

# Или просто make, если используете Makefile
make
```

### Запуск программы

```bash
    # Запуск из директории build
    ./FileManager

    # Или с указанием пути
    ./FileManager /путь/к/директории
```

## 📌 Основные команды

### Навигация

cd [путь]      - Перейти в указанную директорию
cd ..          - Перейти на уровень выше
cd /           - Перейти в корневую директорию
pwd            - Показать текущий путь

### Просмотр файлов

ls             - Показать содержимое текущей директории
ls -a          - Показать все файлы (включая скрытые)
ls -l          - Подробный список файлов
tree           - Показать дерево директорий

### Операции с файлами

cat [файл]     - Показать содержимое файла
info [файл]    - Информация о файле
copy [ист] [назн] - Копировать файл/директорию
move [ист] [назн] - Переместить файл/директорию
del [файл]     - Удалить файл
mkdir [имя]    - Создать директорию

### Системные команды

help           - Показать справку по командам
clear          - Очистить экран
exit           - Выйти из программы
history        - Показать историю команд

## 🎯 Примеры использования

### Пример 1: Базовая навигация

```bash
    $ ./FileManager
    > pwd
    /home/user
    > ls
    Documents/ Downloads/ Pictures/
    > cd Documents
    > ls
    report.txt  project/
    > cd ..
```

### Пример 2: Просмотр информации о файлах

```bash
    > ls -l
    drwxr-xr-x  user  group  4096  Jan 10 10:30 Documents/
    -rw-r--r--  user  group   512  Jan 10 09:15 notes.txt
    > info notes.txt
    Имя: notes.txt
    Размер: 512 байт
    Изменен: 2024-01-10 09:15:00
    Права: -rw-r--r--
```

### Пример 3: Работа с файлами

```bash
    > cat notes.txt
    Это содержимое файла...
    > copy notes.txt backup.txt
    Файл скопирован
    > mkdir archive
    Директория создана
    > move backup.txt archive/
    Файл перемещен
```

## ⚙️ Параметры командной строки

```bash
    # Запуск с указанием начальной директории
    ./FileManager /путь/к/рабочей/директории

    # Запуск с логгированием
    ./FileManager --log debug.log

    # Показать версию
    ./FileManager --version

    # Показать справку
    ./FileManager --help
```

Доступные параметры:
    - `--help`, `-h`     - Показать эту справку
    - `--version`, `-v`  - Показать версию программы
    - `--log [файл]`     - Включить запись логов в файл
    - `--config [файл]`  - Использовать указанный конфигурационный файл

## 🔧 Конфигурация

### Конфигурационный файл

Программа ищет конфигурацию в следующем порядке:
    1. Путь, указанный в параметре `--config`
    2. `~/.filemanager/config.json`
    3. `/etc/filemanager/config.json`

### Пример config.json

```json
    {
        "interface": {
            "theme": "dark",
            "show_hidden": false,
            "sort_by": "name"
        },
        "behavior": {
            "confirm_deletes": true,
            "enable_history": true,
            "max_history_size": 100
        }
    }
```

## ❗ Решение проблем

### Частые проблемы

1. **"Permission denied" при запуске**

   ```bash
    chmod +x FileManager
    ./FileManager
   ```

2. **Не найдены библиотеки**
   Убедитесь, что установлены все зависимости:

   ```bash
        # Для Ubuntu/Debian
        sudo apt-get install build-essential cmake
   ```

3. **Ошибка сборки**
   Очистите директорию build и попробуйте снова:

   ```bash
        rm -rf build/*
        cmake ..
        make
   ```

## 📚 Дополнительные возможности

### Горячие клавиши

- `Tab`         - Автодополнение путей
- `Ctrl+C`      - Прервать текущую операцию
- `Ctrl+L`      - Очистить экран
- `Стрелки Вверх/Вниз` - Навигация по истории команд

### Скрипты и пакетная обработка

Программа поддерживает выполнение скриптов:

```bash
    # Файл script.fm
    cd /home/user/Documents
    ls -l
    info report.txt

    # Запуск скрипта
    ./FileManager --script script.fm
```

## 🆘 Получение помощи

Внутри программы:

```bash
    > help           # Общая справка
    > help ls        # Справка по конкретной команде
    > help navigation # Справка по категории команд
```

Вне программы:
    - Исходный код: `Cpp_Project/src/`
    - Документация: `Cpp_Project/docs/`
    - Примеры: `Cpp_Project/examples/`

---

*Последнее обновление: Декабрь 2025*  
*Версия программы: 1.0.0*
